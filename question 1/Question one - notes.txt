1. 
    a. Describe any file handling methods in Python 
        -> open() - it is used in opening files and returning a file object 
        -> read() - used to read the contents of a file 
        ->write() - used to write contents of a file 

    b. explain three visibility specifiers for a class member in python 
        -> public - it could be accessed from anywhere both inside and outside the class
        -> private - its only accessed from inside or within the class
        -> protected  - members in the class are protected wiith a speifier , can be accessed within the class scope and from the classes that inherit the protected class 